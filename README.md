# React + TypeScript + Vite

A simple frontend app to serve small, low-latency models with edge deployment on supported browsers.

## Included models
### Driver Behaviour Classifier
A simple custom CNN classifier trained on the State Farm Distracted Driver dataset.

